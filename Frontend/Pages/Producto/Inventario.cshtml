@page
@model Frontend.Pages.InventarioProductoModel;
@{
    ViewData["Title"] = "Inventario";
    var cont = 0;
}
<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Nombre</th>
            <th scope="col">Cantidad disponible</th>
            <th scope="col">Cantidad de paquetes</th>
            <th scope="col">Cantidad de unidades por paquete</th>
            <th scope="col">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var producto in Model.Productos)
        {
            <form method="POST">
                <tr>
                    <td>@producto.id</td>
                    <td>@producto.nombre</td>
                    <td>@producto.cantidad</td>
                    <td>
                        <select class="form-control" id="exampleFormControlSelect1" name="cantidadPaquetes_@cont" required>
                            @for (int i = 1; i <= 50; i++)
                            {
                                <option>@i</option>
                            }
                        </select>  
                    </td>
                    <td>
                        <select class="form-control" id="exampleFormControlSelect1" name="cantidadProductosPaquete_@cont" required>
                            @for (int i = 0; i <= 50; i++)
                            {
                                <option>@i</option>
                            }
                        </select>  
                    </td>
                    
                    <td>
                        
                        <input type="number" class="form-control d-none" id="id" name="id" placeholder="id" asp-for="@producto.id" />
                        <input type="number" class="form-control d-none" id="cont" name="cont" placeholder="cont" asp-for="@cont" />   
                        <button type="submit"class="btn btn-info" href="">Actualizar</button>
                        
                    </td>
                </tr>
            </form> 
            cont++;
        }
    </tbody>
</table>