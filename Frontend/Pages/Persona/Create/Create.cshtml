@page
@model Pages.FormPersonaModel
@{
    ViewData["Title"] = "Formulario de Persona";
}
<div class="text-center" id = "cuerpo">
    <form method="POST">
        @* <div class="mb-3 row">
            <label for="id" class="col-sm-2 col-form-label">id: </label>
            <div class="col-sm-10">
                <input type="number" class="form-control-plaintext" id="id" name="id" placeholder="id">
            </div>
        </div> *@


        <div class="mb-3 row">
            <label for="nombre" class="col-sm-2 col-form-label">Nombre: </label>
            <div class="col-sm-10">
                <input type="text" class="form-control-plaintext" id="nombre" name="nombre" placeholder="nombre" required>
            </div>
        </div>

        <div class="mb-3 row">
            <label for="cargo" class="col-form-label">Cargo: </label>
            <div class="col-sm-10">
                <select class="form-control" id="exampleFormControlSelect1" name="cargo" required>
                    @foreach (var cargo in Enum.GetNames(typeof(Dominio.TiposCargo)))
                    {
                        <option>@cargo </option>
                    }
                </select>
            </div>
        </div>


        <hr>
            <button type="submit" class="btn btn-success">Enviar</button>
    </form>
</div>